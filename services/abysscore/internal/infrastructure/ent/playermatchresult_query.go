// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/intezya/abyssleague/services/abysscore/internal/infrastructure/ent/match"
	"github.com/intezya/abyssleague/services/abysscore/internal/infrastructure/ent/playermatchresult"
	"github.com/intezya/abyssleague/services/abysscore/internal/infrastructure/ent/predicate"
	"github.com/intezya/abyssleague/services/abysscore/internal/infrastructure/ent/user"
)

// PlayerMatchResultQuery is the builder for querying PlayerMatchResult entities.
type PlayerMatchResultQuery struct {
	config
	ctx        *QueryContext
	order      []playermatchresult.OrderOption
	inters     []Interceptor
	predicates []predicate.PlayerMatchResult
	withMatch  *MatchQuery
	withUser   *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PlayerMatchResultQuery builder.
func (pmrq *PlayerMatchResultQuery) Where(ps ...predicate.PlayerMatchResult) *PlayerMatchResultQuery {
	pmrq.predicates = append(pmrq.predicates, ps...)
	return pmrq
}

// Limit the number of records to be returned by this query.
func (pmrq *PlayerMatchResultQuery) Limit(limit int) *PlayerMatchResultQuery {
	pmrq.ctx.Limit = &limit
	return pmrq
}

// Offset to start from.
func (pmrq *PlayerMatchResultQuery) Offset(offset int) *PlayerMatchResultQuery {
	pmrq.ctx.Offset = &offset
	return pmrq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (pmrq *PlayerMatchResultQuery) Unique(unique bool) *PlayerMatchResultQuery {
	pmrq.ctx.Unique = &unique
	return pmrq
}

// Order specifies how the records should be ordered.
func (pmrq *PlayerMatchResultQuery) Order(o ...playermatchresult.OrderOption) *PlayerMatchResultQuery {
	pmrq.order = append(pmrq.order, o...)
	return pmrq
}

// QueryMatch chains the current query on the "match" edge.
func (pmrq *PlayerMatchResultQuery) QueryMatch() *MatchQuery {
	query := (&MatchClient{config: pmrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pmrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pmrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playermatchresult.Table, playermatchresult.FieldID, selector),
			sqlgraph.To(match.Table, match.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, playermatchresult.MatchTable, playermatchresult.MatchColumn),
		)
		fromU = sqlgraph.SetNeighbors(pmrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (pmrq *PlayerMatchResultQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: pmrq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := pmrq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := pmrq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(playermatchresult.Table, playermatchresult.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, playermatchresult.UserTable, playermatchresult.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(pmrq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first PlayerMatchResult entity from the query.
// Returns a *NotFoundError when no PlayerMatchResult was found.
func (pmrq *PlayerMatchResultQuery) First(ctx context.Context) (*PlayerMatchResult, error) {
	nodes, err := pmrq.Limit(1).All(setContextOp(ctx, pmrq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{playermatchresult.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) FirstX(ctx context.Context) *PlayerMatchResult {
	node, err := pmrq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PlayerMatchResult ID from the query.
// Returns a *NotFoundError when no PlayerMatchResult ID was found.
func (pmrq *PlayerMatchResultQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pmrq.Limit(1).IDs(setContextOp(ctx, pmrq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{playermatchresult.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) FirstIDX(ctx context.Context) int {
	id, err := pmrq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PlayerMatchResult entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PlayerMatchResult entity is found.
// Returns a *NotFoundError when no PlayerMatchResult entities are found.
func (pmrq *PlayerMatchResultQuery) Only(ctx context.Context) (*PlayerMatchResult, error) {
	nodes, err := pmrq.Limit(2).All(setContextOp(ctx, pmrq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{playermatchresult.Label}
	default:
		return nil, &NotSingularError{playermatchresult.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) OnlyX(ctx context.Context) *PlayerMatchResult {
	node, err := pmrq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PlayerMatchResult ID in the query.
// Returns a *NotSingularError when more than one PlayerMatchResult ID is found.
// Returns a *NotFoundError when no entities are found.
func (pmrq *PlayerMatchResultQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = pmrq.Limit(2).IDs(setContextOp(ctx, pmrq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{playermatchresult.Label}
	default:
		err = &NotSingularError{playermatchresult.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) OnlyIDX(ctx context.Context) int {
	id, err := pmrq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PlayerMatchResults.
func (pmrq *PlayerMatchResultQuery) All(ctx context.Context) ([]*PlayerMatchResult, error) {
	ctx = setContextOp(ctx, pmrq.ctx, ent.OpQueryAll)
	if err := pmrq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PlayerMatchResult, *PlayerMatchResultQuery]()
	return withInterceptors[[]*PlayerMatchResult](ctx, pmrq, qr, pmrq.inters)
}

// AllX is like All, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) AllX(ctx context.Context) []*PlayerMatchResult {
	nodes, err := pmrq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PlayerMatchResult IDs.
func (pmrq *PlayerMatchResultQuery) IDs(ctx context.Context) (ids []int, err error) {
	if pmrq.ctx.Unique == nil && pmrq.path != nil {
		pmrq.Unique(true)
	}
	ctx = setContextOp(ctx, pmrq.ctx, ent.OpQueryIDs)
	if err = pmrq.Select(playermatchresult.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) IDsX(ctx context.Context) []int {
	ids, err := pmrq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (pmrq *PlayerMatchResultQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, pmrq.ctx, ent.OpQueryCount)
	if err := pmrq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, pmrq, querierCount[*PlayerMatchResultQuery](), pmrq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) CountX(ctx context.Context) int {
	count, err := pmrq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (pmrq *PlayerMatchResultQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, pmrq.ctx, ent.OpQueryExist)
	switch _, err := pmrq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (pmrq *PlayerMatchResultQuery) ExistX(ctx context.Context) bool {
	exist, err := pmrq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PlayerMatchResultQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (pmrq *PlayerMatchResultQuery) Clone() *PlayerMatchResultQuery {
	if pmrq == nil {
		return nil
	}
	return &PlayerMatchResultQuery{
		config:     pmrq.config,
		ctx:        pmrq.ctx.Clone(),
		order:      append([]playermatchresult.OrderOption{}, pmrq.order...),
		inters:     append([]Interceptor{}, pmrq.inters...),
		predicates: append([]predicate.PlayerMatchResult{}, pmrq.predicates...),
		withMatch:  pmrq.withMatch.Clone(),
		withUser:   pmrq.withUser.Clone(),
		// clone intermediate query.
		sql:  pmrq.sql.Clone(),
		path: pmrq.path,
	}
}

// WithMatch tells the query-builder to eager-load the nodes that are connected to
// the "match" edge. The optional arguments are used to configure the query builder of the edge.
func (pmrq *PlayerMatchResultQuery) WithMatch(opts ...func(*MatchQuery)) *PlayerMatchResultQuery {
	query := (&MatchClient{config: pmrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pmrq.withMatch = query
	return pmrq
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (pmrq *PlayerMatchResultQuery) WithUser(opts ...func(*UserQuery)) *PlayerMatchResultQuery {
	query := (&UserClient{config: pmrq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	pmrq.withUser = query
	return pmrq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MatchID int `json:"match_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PlayerMatchResult.Query().
//		GroupBy(playermatchresult.FieldMatchID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (pmrq *PlayerMatchResultQuery) GroupBy(field string, fields ...string) *PlayerMatchResultGroupBy {
	pmrq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PlayerMatchResultGroupBy{build: pmrq}
	grbuild.flds = &pmrq.ctx.Fields
	grbuild.label = playermatchresult.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MatchID int `json:"match_id,omitempty"`
//	}
//
//	client.PlayerMatchResult.Query().
//		Select(playermatchresult.FieldMatchID).
//		Scan(ctx, &v)
func (pmrq *PlayerMatchResultQuery) Select(fields ...string) *PlayerMatchResultSelect {
	pmrq.ctx.Fields = append(pmrq.ctx.Fields, fields...)
	sbuild := &PlayerMatchResultSelect{PlayerMatchResultQuery: pmrq}
	sbuild.label = playermatchresult.Label
	sbuild.flds, sbuild.scan = &pmrq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PlayerMatchResultSelect configured with the given aggregations.
func (pmrq *PlayerMatchResultQuery) Aggregate(fns ...AggregateFunc) *PlayerMatchResultSelect {
	return pmrq.Select().Aggregate(fns...)
}

func (pmrq *PlayerMatchResultQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range pmrq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, pmrq); err != nil {
				return err
			}
		}
	}
	for _, f := range pmrq.ctx.Fields {
		if !playermatchresult.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if pmrq.path != nil {
		prev, err := pmrq.path(ctx)
		if err != nil {
			return err
		}
		pmrq.sql = prev
	}
	return nil
}

func (pmrq *PlayerMatchResultQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PlayerMatchResult, error) {
	var (
		nodes       = []*PlayerMatchResult{}
		_spec       = pmrq.querySpec()
		loadedTypes = [2]bool{
			pmrq.withMatch != nil,
			pmrq.withUser != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PlayerMatchResult).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PlayerMatchResult{config: pmrq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, pmrq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := pmrq.withMatch; query != nil {
		if err := pmrq.loadMatch(ctx, query, nodes, nil,
			func(n *PlayerMatchResult, e *Match) { n.Edges.Match = e }); err != nil {
			return nil, err
		}
	}
	if query := pmrq.withUser; query != nil {
		if err := pmrq.loadUser(ctx, query, nodes, nil,
			func(n *PlayerMatchResult, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (pmrq *PlayerMatchResultQuery) loadMatch(ctx context.Context, query *MatchQuery, nodes []*PlayerMatchResult, init func(*PlayerMatchResult), assign func(*PlayerMatchResult, *Match)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PlayerMatchResult)
	for i := range nodes {
		fk := nodes[i].MatchID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(match.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "match_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (pmrq *PlayerMatchResultQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*PlayerMatchResult, init func(*PlayerMatchResult), assign func(*PlayerMatchResult, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*PlayerMatchResult)
	for i := range nodes {
		fk := nodes[i].PlayerID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "player_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (pmrq *PlayerMatchResultQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := pmrq.querySpec()
	_spec.Node.Columns = pmrq.ctx.Fields
	if len(pmrq.ctx.Fields) > 0 {
		_spec.Unique = pmrq.ctx.Unique != nil && *pmrq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, pmrq.driver, _spec)
}

func (pmrq *PlayerMatchResultQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(playermatchresult.Table, playermatchresult.Columns, sqlgraph.NewFieldSpec(playermatchresult.FieldID, field.TypeInt))
	_spec.From = pmrq.sql
	if unique := pmrq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if pmrq.path != nil {
		_spec.Unique = true
	}
	if fields := pmrq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, playermatchresult.FieldID)
		for i := range fields {
			if fields[i] != playermatchresult.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if pmrq.withMatch != nil {
			_spec.Node.AddColumnOnce(playermatchresult.FieldMatchID)
		}
		if pmrq.withUser != nil {
			_spec.Node.AddColumnOnce(playermatchresult.FieldPlayerID)
		}
	}
	if ps := pmrq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := pmrq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := pmrq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := pmrq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (pmrq *PlayerMatchResultQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(pmrq.driver.Dialect())
	t1 := builder.Table(playermatchresult.Table)
	columns := pmrq.ctx.Fields
	if len(columns) == 0 {
		columns = playermatchresult.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if pmrq.sql != nil {
		selector = pmrq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if pmrq.ctx.Unique != nil && *pmrq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range pmrq.predicates {
		p(selector)
	}
	for _, p := range pmrq.order {
		p(selector)
	}
	if offset := pmrq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := pmrq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PlayerMatchResultGroupBy is the group-by builder for PlayerMatchResult entities.
type PlayerMatchResultGroupBy struct {
	selector
	build *PlayerMatchResultQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (pmrgb *PlayerMatchResultGroupBy) Aggregate(fns ...AggregateFunc) *PlayerMatchResultGroupBy {
	pmrgb.fns = append(pmrgb.fns, fns...)
	return pmrgb
}

// Scan applies the selector query and scans the result into the given value.
func (pmrgb *PlayerMatchResultGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pmrgb.build.ctx, ent.OpQueryGroupBy)
	if err := pmrgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerMatchResultQuery, *PlayerMatchResultGroupBy](ctx, pmrgb.build, pmrgb, pmrgb.build.inters, v)
}

func (pmrgb *PlayerMatchResultGroupBy) sqlScan(ctx context.Context, root *PlayerMatchResultQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(pmrgb.fns))
	for _, fn := range pmrgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*pmrgb.flds)+len(pmrgb.fns))
		for _, f := range *pmrgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*pmrgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pmrgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PlayerMatchResultSelect is the builder for selecting fields of PlayerMatchResult entities.
type PlayerMatchResultSelect struct {
	*PlayerMatchResultQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (pmrs *PlayerMatchResultSelect) Aggregate(fns ...AggregateFunc) *PlayerMatchResultSelect {
	pmrs.fns = append(pmrs.fns, fns...)
	return pmrs
}

// Scan applies the selector query and scans the result into the given value.
func (pmrs *PlayerMatchResultSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, pmrs.ctx, ent.OpQuerySelect)
	if err := pmrs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PlayerMatchResultQuery, *PlayerMatchResultSelect](ctx, pmrs.PlayerMatchResultQuery, pmrs, pmrs.inters, v)
}

func (pmrs *PlayerMatchResultSelect) sqlScan(ctx context.Context, root *PlayerMatchResultQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(pmrs.fns))
	for _, fn := range pmrs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*pmrs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := pmrs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
