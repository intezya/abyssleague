// Code generated by ent, DO NOT EDIT.

package ent

import (
	"abysscore/internal/infrastructure/ent/user"
	"abysscore/internal/infrastructure/ent/userbalance"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// UserBalance is the model entity for the UserBalance schema.
type UserBalance struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// UserID holds the value of the "user_id" field.
	UserID int `json:"user_id,omitempty"`
	// Coins holds the value of the "coins" field.
	Coins float64 `json:"coins,omitempty"`
	// LastUpdated holds the value of the "last_updated" field.
	LastUpdated time.Time `json:"last_updated,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserBalanceQuery when eager-loading is set.
	Edges        UserBalanceEdges `json:"edges"`
	selectValues sql.SelectValues
}

// UserBalanceEdges holds the relations/edges for other nodes in the graph.
type UserBalanceEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserBalanceEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserBalance) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userbalance.FieldCoins:
			values[i] = new(sql.NullFloat64)
		case userbalance.FieldID, userbalance.FieldUserID:
			values[i] = new(sql.NullInt64)
		case userbalance.FieldLastUpdated, userbalance.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserBalance fields.
func (ub *UserBalance) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userbalance.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ub.ID = int(value.Int64)
		case userbalance.FieldUserID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value.Valid {
				ub.UserID = int(value.Int64)
			}
		case userbalance.FieldCoins:
			if value, ok := values[i].(*sql.NullFloat64); !ok {
				return fmt.Errorf("unexpected type %T for field coins", values[i])
			} else if value.Valid {
				ub.Coins = value.Float64
			}
		case userbalance.FieldLastUpdated:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_updated", values[i])
			} else if value.Valid {
				ub.LastUpdated = value.Time
			}
		case userbalance.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				ub.CreatedAt = value.Time
			}
		default:
			ub.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserBalance.
// This includes values selected through modifiers, order, etc.
func (ub *UserBalance) Value(name string) (ent.Value, error) {
	return ub.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserBalance entity.
func (ub *UserBalance) QueryUser() *UserQuery {
	return NewUserBalanceClient(ub.config).QueryUser(ub)
}

// Update returns a builder for updating this UserBalance.
// Note that you need to call UserBalance.Unwrap() before calling this method if this UserBalance
// was returned from a transaction, and the transaction was committed or rolled back.
func (ub *UserBalance) Update() *UserBalanceUpdateOne {
	return NewUserBalanceClient(ub.config).UpdateOne(ub)
}

// Unwrap unwraps the UserBalance entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ub *UserBalance) Unwrap() *UserBalance {
	_tx, ok := ub.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserBalance is not a transactional entity")
	}
	ub.config.driver = _tx.drv
	return ub
}

// String implements the fmt.Stringer.
func (ub *UserBalance) String() string {
	var builder strings.Builder
	builder.WriteString("UserBalance(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ub.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", ub.UserID))
	builder.WriteString(", ")
	builder.WriteString("coins=")
	builder.WriteString(fmt.Sprintf("%v", ub.Coins))
	builder.WriteString(", ")
	builder.WriteString("last_updated=")
	builder.WriteString(ub.LastUpdated.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(ub.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// UserBalances is a parsable slice of UserBalance.
type UserBalances []*UserBalance
